# Agent 使用说明（chatgpt.com/codex）

本项目目标：

最优化问题
1)有N个芯片，每个芯片有接收和发送各d个端口，芯片之间用这d个端口的全部或部分进行互联；
2) N个芯片分成2组，第1组芯片数为a, 第2组芯片数为b, a+b=N;
3)数据流向：数据从第一组的每个芯片流入第2组的每个芯片（a对b）；
4）每次通信的数据量相同； 
5）每个端口的通信带宽相同；
6）通信时间=最后完成通信时间-通信开始时间
求芯片间的最佳连接方式，使得流水线的通信时间最小；

---

## 1. Agent 的核心任务

0. **初始化**

导入如下参数
N=128
a=64
b=64
d=12

1. **寻优方案**，
我初步考虑了如下初始方案，供参考，也可以修改或设计其他方案
首先要生成通信图，描述N个芯片作为源和目的的通信关系，输出文件，可以重复使用；
然后确定拓扑的表达方式，可以用图来表示；
接着构建基于确定的拓扑，生成通信时间的仿真工具；
最后开始寻优过程
  初始化拓扑
  循环调用 拓扑优化工具，直到通信时间达到最小值；

2. **工程化增强**
   - N个芯片作为源和目的的通信关系，输出文件，可以重复使用
   - 导出拓扑为 CSV/JSON 供其他模拟器使用。
   - 加日志、CLI 参数（argparse）、实验记录（JSON/SQLite）。
   - 加可视化（networkx + matplotlib / graphviz）。

3. **分析结果**
   - 解释不同拓扑的 metric 差异。
   
---

## 2. 一些约定（建议你和 Agent 一起遵守）

1. **所有全局参数统一放在 `topo_config.py`**
   - 包括：N, a, b等。

2. **涉及关键逻辑的修改，要求 Agent 给出文字说明**

3. **导出/可视化逻辑尽量放在单独文件**
   - 如 `export_topology.py`, `visualize_topology.py`，避免污染核心搜索逻辑。

4. **运行与汇报约定（经验沉淀）**
   - 每次修改后都运行 `python cli.py optimize --iterations <n> --seed 42 --output optimization_result.json` 并与交换机基线对比。
   - Summary 必须包含：
     - **优化结果**：通信时间与 switch baseline 对比。
     - **Topology (edge list)**：`[[1,17],[1,16],...]` 格式。
     - **通信关系矩阵**：按矩阵输出，行=源芯片，列=目的芯片，1 表示有通信，0 表示无通信。
     - **端口利用率**：按每个芯片输出 (真实传递数据量 ÷ 端口总通信能力) 以评估优化潜力。
     - **迭代曲线**：迭代次数 vs 通信时间，可视化图片路径。
     - **迭代过程时间序列**：每次迭代的通信时间列表。
     - **拓扑图与导出**：拓扑图路径 + draw.io 与 SVG 导出路径。
   - 优化需要复用历史结果：保存历史最优拓扑作为起始条件，并基于历史表现决定提前停止迭代。
   - 优化过程需输出关键日志：通信关系矩阵、迭代通信时间序列、最终结果摘要与拓扑图/导出路径。
   - 版本 1 的结果表现最好，后续寻优可基于此继续改进。

---


这样 Agent 就会围绕你关心的方向持续演化这个项目。
